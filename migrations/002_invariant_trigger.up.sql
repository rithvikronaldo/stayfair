-- Populated Thu Apr 23 (Week 1 · Day 4 per execution plan).
-- Will define check_transaction_balanced() + AFTER INSERT ON entries
-- DEFERRABLE INITIALLY DEFERRED constraint trigger that enforces
-- Σ in = Σ out per currency at COMMIT.
SELECT 1;
